# Hermes 专家市场本地接入配置（已生成的本地运行模板）
# 启动 dev 时 Node 会自动加载本文件
# 重启后需重新加载；不要提交到 git

baseUrl: "https://market.quant-speed.com/"

cacheTtlSeconds: 30

# localPackagesRoot 留空将走默认：${HERMES_WEBUI_STATE_DIR}/experts/packages
# localPackagesRoot: "${HERMES_WEBUI_STATE_DIR}/experts/packages"

clientIdTemplate: "hermes-web-ui-v{version}-user-{userId}"

maxPackageBytes: 104857600      # 100 MiB

downloadTimeoutMs: 60000
verifyTimeoutMs: 30000
