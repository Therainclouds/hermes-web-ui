# 11. 看板

**路由**：`/hermes/kanban`

> [← 返回索引](./README.md)

Agent 任务规划与跟踪看板（profile 隔离）。

## 默认九个状态列

`triage` → `todo` → `scheduled` → `ready` → `running` → `blocked` → `review` → `done` → `archived`

## 操作

- **新建任务**：右上角「+ 新建」→ 填标题、描述、指派人、初始状态。
- **拖拽**：在状态列之间拖动卡片改变状态。
- **点开卡片**：右侧抽屉显示详情 + 评论 + 子任务。
- **创建看板**：默认看板不可删，可创建自己的副板（顶部「看板」下拉切换）。
- **多看板切换**：URL `?board=xxx` 或顶部下拉。
- **自动刷新**：默认定时刷新任务状态（可关）。
