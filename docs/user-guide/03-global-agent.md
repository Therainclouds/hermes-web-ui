# 3. 全局 Agent

**路由**：`/hermes/global-agent`

> [← 返回索引](./README.md)

与「AI 对话」共用 ChatPanel 组件，但绑定到一个 **共享的全局会话上下文**：

- 所有用户/会话在同一全局会话池中可被 Agent 检索和引用。
- 适合做「跨会话长期记忆 + 检索增强」的场景。
- 操作方式与会话页一致：新建全局会话、消息收发、文件上传、会话搜索均通用。

详见 [AI 对话](./02-chat.md) 章节的操作清单。
