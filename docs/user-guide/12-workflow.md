# 12. 工作流

**路由**：`/hermes/workflow`

> [← 返回索引](./README.md)

可视化多 Agent 协作流程图编辑器（基于 Vue Flow）。

## 核心能力

- **节点类型**：起始、Agent、工具、条件、结束等。
- **连线**：拖拽节点边框到目标节点建连接，附带条件表达式。
- **侧边聊天**：右侧抽屉集成聊天面板，可在编辑时直接对 Agent 调试节点。
- **保存 / 另存为新版本**：每次保存生成版本号。
- **立即运行**：点工具栏「▶ 运行」触发；实时查看节点状态、Token 用量。
- **运行记录**：下方面板可看历史 run，从指定节点重新运行（rerun-from-node）。
- **停止运行**：运行中可手动终止。
- **批量操作**：多选后批量删除工作流。
