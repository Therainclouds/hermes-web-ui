# 18. 记忆

**路由**：`/hermes/memory`

> [← 返回索引](./README.md)

维护 Agent 的长期记忆（三段 Markdown）：

| 段落 | 用途 |
| --- | --- |
| `memory` | 通用长期事实 |
| `user` | 用户偏好与画像 |
| `soul` | Agent 人设与价值观 |

## 操作

- 只读浏览：默认折叠为卡片预览。
- 点「编辑」进入 Markdown 编辑器 → 保存立即生效。
- 切换 profile 后看到的是该 profile 独立的记忆文件。
